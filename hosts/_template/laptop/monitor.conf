# -----------------------------------------------------
# MONITOR CONFIGURATION (LAPTOP TEMPLATE)
# -----------------------------------------------------
# Use 'hyprctl monitors' to identify your display name.

# Example: Internal Display
monitor = eDP-1, 1920x1080@60, 0x0, 1

# Example: External HDMI Display (Top)
# monitor = HDMI-A-1, preferred, auto-up, 1.2
